using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[CreateAssetMenu(fileName = "ButtonDataBase", menuName = "CustomScriptableObject/ButtonDataBase")]
public class ButtonDataBase : ScriptableObject
{
    public bool canOpenSetting;
    public bool canCloseSetting;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }


}
